import 'package:flutter/material.dart';

class PendienteScreen extends StatelessWidget {
  const PendienteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Pendiente')));
  }
}
